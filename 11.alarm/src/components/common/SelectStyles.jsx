import styled from "styled-components";

export default styled.label`
  select {
    font-family: "Jua", sans-serif;
    font-size: 1.5rem;
    border: none;
  }

  select:focus {
    outline: none;
  }
`
